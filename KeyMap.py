from CamTypes import *

# Use int values for keys if you run out of keyboard space
# or dont want a mode bound to a key

# Cant use ¬ or Esc as these keys are already bound

keymap = {'w' : Full,
          'e' : Dvd,
          "r" : Random,
          "t" : Solitaire,
          "y" : Mirror,
          "u" : SolitaireGravity,
          "i" : Slow,
          "o" : DvdMulti,
          }

